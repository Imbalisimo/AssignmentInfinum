@model PagedList.IPagedList<contacts>
@using PagedList.Mvc;

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table id="tblContacts" class="table">
    <tr>
        <th>
            Name
        </th>
        <th>
            Birth date
        </th>
        <th>
            Address
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td class="Name">
                <span>@item.name</span>
                <input type="text" value="@item.name" style="display:none" />
            </td>
            <td class="BirthDate">
                <span>@item.birthdate</span>
                <input type="text" value="@item.birthdate" style="display:none" />
            </td>
            <td class="Address">
                <span>@item.address</span>
                <input type="text" value="@item.address" style="display:none" />
            </td>
            <td>
                <a class="Edit" href="javascript:;">Edit</a>
                <a class="Update" href="javascript:;" style="display:none">Update</a>
                <a class="Cancel" href="javascript:;" style="display:none">Cancel</a>
                <input type="button" value="Delete"
                       onClick="deleteRow(@item.name,@item.birthdate,@item.address)">
            </td>
        </tr>
    }
    <tr>Contacts count: @Model.Count()</tr>

</table>
<br />
Page @(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber) of @Model.PageCount
@Html.PagedListPager(Model, page => Url.Action("Index", new { page }))

<table border="0" cellpadding="0" cellspacing="0">
    <tr>
        <td style="width: 150px">
            Name<br />
            <input type="text" id="txtName" style="width:140px" />
        </td>
        <td style="width: 150px">
            Birth date:<br />
            <input type="text" id="txtBirthDate" style="width:140px" />
        </td>
        <td style="width: 150px">
            Address:<br />
            <input type="text" id="txtAddress" style="width:140px" />
        </td>
        <td style="width: 200px">
            <br />
            <input type="button" id="btnAdd" value="Add" />
        </td>
    </tr>
</table>

@section scripts {
    <script type="text/javascript" src="https://ajax.googleapis.com/ajax/libs/jquery/1.8.3/jquery.min.js"></script>
    <script type="text/javascript" src="https://ajax.cdnjs.com/ajax/libs/json2/20110223/json2.js"></script>
    <!--Script references. -->
    <!--The jQuery library is required and is referenced by default in _Layout.cshtml. -->
    <!--Reference the SignalR library. -->
    <script src="~/Scripts/jquery.signalR-2.2.2.min.js"></script>
    <!--Reference the autogenerated SignalR hub script. -->
    <script src="~/signalr/hubs"></script>
    <!--SignalR script to update the chat page and send messages.-->
    <script>
        $(function () {
            // Reference the auto-generated proxy for the hub.
            var chat = $.connection.contactsHub;
            // Create a function that the hub can call back to display messages.
            chat.client.sendNewContact = function (name, birth, address) {
                // Add the message to the page.

                //TO-DO: APPEND ROW!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
                $('#discussion').append('<li><strong>' + htmlEncode(name)
                    + '</strong>: ' + htmlEncode(message) + '</li>');
            };
            // Start the connection.
            $.connection.hub.start().done(function () {
                $('#sendmessage').click(function () {
                    // Call the Send method on the hub.
                    chat.server.send($('#txtName').val(), $('#txtBirthDate').val(), ('#txtAddress').val());
                });
            });
        });
        // This optional function html-encodes messages for display in the page.
        function htmlEncode(value) {
            var encodedValue = $('<div />').text(value).html();
            return encodedValue;
        }
    </script>
    <script type="text/javascript">
   $(document).ready(function(){
    $("#btnAdd").click(function (){
        var txtName = $("#txtName");
      var txtBirthDate = $("#txtBirthDate");
      var txtAddress = $("#txtAddress");
      console.log("This script is working, button add");

      $.ajax({
      type: "POST",
      url: "http://localhost:44325/Contacts/InsertContact",
      data: formToJSON(txtName.val(),txtBirthDate.val(),txtAddress.val()),
      contentType: "application/json", /*; charset=utf-8*/
      dataType: "json",
      success: function (r) {
          console.log("succesfully ran add");

      }
      });
    });
	});

  function formToJSON(txtName,txtBirthDate,txtAddress){
    return JSON.stringify({
      "birthdate":txtBirthDate,
      "address":txtAddress,
          "name":txtName
    });
        }

        // This optional function html-encodes messages for display in the page.
  function htmlEncode(value) {
      var encodedValue = $("<div />").text(value).html();
      return encodedValue;
        }

        function deleteRow(name, datebirth, address) {
            $.ajax({
                type: "POST",
                url: "http://localhost:8080/Contacts/DeleteContact",
                data: formToJSON(name, datebirth, address),
                contentType: "application/json", /*; charset=utf-8*/
                dataType: "json",
                success: function (r) {
                    console.log("succesfully ran add");
                }
            });
        }

        function rowToHtml(name, datebirth, address) {
            return "<tr>" + "<td class=" + '"Name"' + ">"
                + "<span>" + name + "</span>"
                + "<input type=" + '"text"' + "value=" + '"' + name + '"' + " style=" + '"display:none"' + "/>"
                + "</td>" +
                "<td class=" + '"BirthDate"' + ">" +
                + "<span>" + datebirth + "</span>" +
                + "<input type=" + '"text"' + "value=" + '"' + datebirth + '"' + " style=" + '"display:none"' + "/>"
                + "</td>" +
                "<td class=" + '"Address"' + ">"
                + "<span>" + address + "</span>"
                + "<input type=" + '"text"' + "value=" + '"' + address + '"' + " style=" + '"display:none"' + "/>"
                + "</td>" +
                "<td>" +
                "<input type=" + '"button"' + "value=" + "'Delete'" + "onClick=" + '"deleteRow(' + name + "," + datebirth + "," + address + ')"' +
                + "/>"

                + "</td>";
        }
    </script>
}
